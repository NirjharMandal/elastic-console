<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- App Sandbox (required for Mac App Store / notarized sandboxed builds) -->
  <key>com.apple.security.app-sandbox</key>
  <true/>

  <!-- Outbound network access so the Rust layer can reach Elasticsearch -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- Keychain access group so the `keyring` crate can store ES credentials
       under the sandbox. Replace TEAMID via $(AppIdentifierPrefix) at sign time. -->
  <key>keychain-access-groups</key>
  <array>
    <string>$(AppIdentifierPrefix)com.elasticconsole.app</string>
  </array>
</dict>
</plist>
