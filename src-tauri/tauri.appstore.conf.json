{
  "$schema": "https://schema.tauri.app/config/2",
  "//": "Mac App Store overlay — merge with: tauri build --config src-tauri/tauri.appstore.conf.json --target universal-apple-darwin. Fill the signing identities / provider before use. NOT required for the default unsigned local build.",
  "bundle": {
    "targets": ["app"],
    "macOS": {
      "entitlements": "entitlements.plist",
      "minimumSystemVersion": "10.15",
      "signingIdentity": "3rd Party Mac Developer Application: YOUR NAME (TEAMID)",
      "providerShortName": "YOURPROVIDER",
      "files": {
        "embedded.provisionprofile": "embedded.provisionprofile"
      }
    }
  }
}
